export type Attributes = {
    originalImageId: number
    currentImageId: number
}
export type WpImage = {
    source_url: string
    media_details: {
        height: number
        width: number
    }
}
export type PhotonImage = T
